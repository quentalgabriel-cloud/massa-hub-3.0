# components — UI

- `ui/` — componentes atômicos, sem lógica de negócio
- `oportunidades/` — componentes do módulo de oportunidades

Server Components por padrão; `'use client'` só quando houver interatividade. Tokens via `globals.css`.
