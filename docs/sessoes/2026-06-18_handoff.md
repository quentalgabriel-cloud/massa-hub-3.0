## Sessão 2026-06-18 — Handoff para novo agente

### Estado do repositório
- Branch de trabalho: `claude/charming-noether-yhui6p` (commitado e pushado)
- Repo: `quentalgabriel-cloud/massa-hub-3.0`
- Working tree: limpo. Último commit: `8b7eebb` — scaffold Ciclo 0.
- Stack: Next.js 15.5.19 + Tailwind v4 + Vitest 3.2.6 + TypeScript strict

### Como se orientar (leitura mínima — nesta ordem)
1. `CLAUDE.md` — estratégia, produto, quarentenas, Anti-Padrão #1
2. `AGENTS.md` — convenções técnicas, sequência obrigatória, paralelismo seguro
3. `docs/decisoes/log.md` — decisões D1..D11 + deadline YOUPIX 29/set/2026
4. `docs/sessoes/2026-06-18_handoff.md` — este arquivo (estado atual)
5. `docs/specs/01-reputacao-lastro.md` — leitura obrigatória para o Ciclo 1

Não é preciso explorar o codebase — scaffold limpo e documentado.

### O que foi construído nesta sessão (commits em ordem)

**Harness de dev (Fase 0):**
- `AGENTS.md` — convenções técnicas para subagentes: estrutura hexagonal, regras de
  import (domínio nunca importa de infra), sequência obrigatória, paralelismo seguro vs
  não-seguro, checklist pré-merge 2 camadas, tokens apontando para globals.css
- `.claude/hooks/session-end.sh` — hook que salva resumo em docs/sessoes/ ao fim de
  cada sessão remota
- `docs/sessoes/.gitkeep` — pasta de memória de sessão

**Trilha B — identidade visual:**
- `docs/specs/06-identidade-visual-pesquisa.md` — sprint completo (7 entregáveis,
  método em 4 etapas, régua de qualidade) + destilação §4.1 já registrada:
  - ADOTADO: tese "Lastro × Ritmo = um registro que pulsa" como ideia organizadora e
    máquina de decisão. O fosso real é o sistema (mono + léxico + ideia), não a cor.
    6 princípios generativos de design.
  - EM PROVA (só oficializar na Screen 10): violeta #5A43F0, paper #FAF7F0, âmbar como
    co-protagonista com regra de proporção (uma cor lidera por tela), Ritmo em âmbar

**Scaffold — Ciclo 0:**
- `package.json` — Next.js 15.5.19, Tailwind v4, Vitest 3.2.6, Zod, TypeScript
- `tsconfig.json` — strict, aliases @dominio/@aplicacao/@infra apontando para src/domain etc.
- `vitest.config.ts` — testes de domínio em node puro (sem banco, sem rede)
- `src/app/globals.css` — FONTE UNICA de tokens (@theme Tailwind v4). Tokens oficiais
  do spec 04 (violeta). Tokens em prova NAO entram aqui ate Screen 10.
- `src/app/layout.tsx` + `src/app/page.tsx` — layout com as 3 fontes, pagina placeholder
- `src/domain/README.md`, `src/domain/ports/README.md`
- `src/application/README.md`, `src/infrastructure/README.md`, `src/components/README.md`
- `tests/domain/scaffold.test.ts` — teste-fumaca passando
- `npm run build` ok / `npm run test:dominio` ok

**Consolidacao de tokens:**
- `AGENTS.md §8` aponta para globals.css em vez de duplicar hex
- `docs/specs/04-design-system.md` recebe nota de fonte unica

### Decisoes tomadas nesta sessao

- Nomenclatura do scaffold: pastas em ingles (canonico hexagonal) + entidades em PT-BR.
  Nao renomear. READMEs de 1 linha em cada pasta explicam em PT.
- design.md + Stitch (Google Labs): analisados. Decisao: adotar principio (fonte unica),
  recusar tooling (escala que nao existe). Resolvido via globals.css nativo.
- Ciclo de implementacao definido — para cada modulo da fila (PROMPTS.md):
  1. dominio + testes (sem banco, sem rede)
  2. casos de uso (mocks das portas)
  3. adapters (Supabase // Anthropic — paralelos so entre si)
  4. UI (Server Components → casos de uso)
  5. evals (so modulo de IA)
  → freio de escopo + fim-de-sessao a cada volta

### Ciclo 1 — CONCLUIDO (PROMPT 3): dominio do Lastro

Modelado o dominio da reputacao, sem UI e sem Supabase. `npm run test:dominio`
verde (16 testes) e `tsc --noEmit` limpo. Entregue:
- `src/domain/prova/Prova.ts` — entidade. Campos: id, tipo, titulo, descricao,
  criadorId (autor), contratanteId (marca), resultado, participantes (grafo),
  assinaturas, data. **status DERIVADO** (`em_andamento | verificada`), nunca
  atribuido a mao. `assinar()` e imutavel (retorna nova prova).
- `src/domain/prova/Assinatura.ts` — value object: autorId, papel
  (criador | contratante), assinadoEm.
- `src/domain/prova/Lastro.ts` — VO de fatos contaveis: nProvasVerificadas,
  marcasDistintas, marcasRecorrentes, nRecomendacoes. NUNCA score (D1).
- `src/domain/ports/ProvaRepositorio.ts` — porta: buscarPorId, salvar, listarPorCriador.
- `tests/domain/prova/{Prova,Lastro}.test.ts`.

Regra-nucleo implementada: Prova so e `verificada` quando assinada pelos DOIS
lados (um criador + um contratante) — bilateral, implica >= 2 assinaturas.

**Correcao de contrato:** o enum de status fixado e `em_andamento | verificada`
(spec 01, fonte da verdade). A lista de 4 estados que aparecia aqui antes
(rascunho | pendente | verificada | rejeitada) foi descartada.

### Persistencia da Prova — FEITO (infra)

- Projeto Supabase criado: `massa-hub-3.0`, org `massa-hub`, regiao `sa-east-1`
  (Sao Paulo), ref `bmwlxjtuhwrqvacnvnhc`, custo R$ 0/mes.
- Migracao `ciclo_1_provas_assinaturas` aplicada: tabelas `provas` e `assinaturas`
  (FK + unique por autor), RLS ligado em ambas. SEM coluna de status (derivado no
  dominio). Sem politicas publicas ainda (acesso server-side via service role).
- Adapter `src/infrastructure/supabase/ProvaRepositorioSupabase.ts` + factory
  `cliente.ts` implementando a porta. tsc --noEmit limpo.
- `.env.example` ja traz URL e anon/publishable key (publicas) do projeto.

PENDENCIA que destrava o teste de integracao: copiar a `SUPABASE_SERVICE_ROLE_KEY`
do painel (Project Settings > API) para o `.env.local` — o MCP nao expoe a chave
secreta. So com ela o adapter roda contra o banco de verdade.

### Ciclo 2 — dominio FEITO (parte 1 de 3)

Modelado o dominio do modulo de oportunidades, sem IA e sem banco. test:dominio
verde (34 no total), tsc --noEmit limpo. Entregue:
- `src/domain/oportunidade/Oportunidade.ts` — entidade. status
  (aberta | em_selecao | fechada), transicoes imutaveis, candidatar(). GATE: so
  publica com origem.revisadoPeloAutor === true (spec 02).
- `Squad.ts`, `Papel.ts`, `FaixaSeguidores.ts` — VOs. Papel = { funcao, qtd,
  nicho?, faixaSeguidores? } reconciliando spec 02 (simples) e 05 (superset).
- `TicketExtraido.ts` — VO da proposta da IA (pre-revisao). `confianca` 0..1 e
  sinal de EXTRACAO, nao score de reputacao (nao fere D1).
- Portas `ExtratorDeTicket.ts` (saida p/ IA) e `OportunidadeRepositorio.ts`.

### Proximo passo — Ciclo 2 (partes 2 e 3)

2. Adapter `infrastructure/anthropic/ExtratorDeTicketAnthropic.ts` validando a
   saida do modelo com Zod contra TicketExtraido + harness `eval/ticket/` com
   briefings REAIS do Gabriel (spec 05: o system prompt se constroi iterando,
   nao no chute). PRECISA da ANTHROPIC_API_KEY.
3. Adapter Supabase `OportunidadeRepositorioSupabase` + schema (espelha o Ciclo 1).

### Fila de ciclos

| Ciclo | PROMPT | Descricao | Status |
|-------|--------|-----------|--------|
| 0 | 0.5 | Scaffold hexagonal + tokens fonte unica | Concluido |
| 1 | P3 | Dominio: Prova + Assinatura + Lastro + testes | Concluido |
| 2 | P4 | Dominio Oportunidade + adapter Anthropic (extracao IA) | PROXIMO |
| 3 | P5 | Screen 10 — tela de publicar oportunidade | — |
| 4 | P6 | Mercado de oportunidades (workspace 2 paineis) | — |

### Pendencias manuais do Gabriel (nao travam o Ciclo 1)

Duas acoes bloqueadas pelo auto mode do Claude Code:

```bash
# 1. Tornar o hook executavel (no terminal do projeto)
chmod +x .claude/hooks/session-end.sh

# 2. Adicionar no .claude/settings.json apos o bloco SessionStart existente:
,
"Stop": [
  {
    "hooks": [{"type": "command",
               "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-end.sh"}]
  }
]
```

### Contexto de deadline

YOUPIX Summit 2026: 29/set/2026 (Sao Paulo). Criterios de go: modulo de oportunidades
em beta, 20-50 oportunidades reais, 200-500 perfis (foco em assessores), 2-3 agencias
testando. Pagamento do estande ate jul/2026. Se nao atingir: ir como visitante, 2027.
